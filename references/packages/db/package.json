{
  "name": "@spectral/db",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "type": "module",
  "scripts": {
    "db:gen": "npx prisma generate && rm -rf dist && npx tsc && npx @better-auth/cli generate --config ../../apps/web/app/server/services/auth/db.server.ts -y ",
    "db:migrate": "npx prisma migrate dev && npx prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "@better-auth/cli": "^1.2.10"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  }
}
